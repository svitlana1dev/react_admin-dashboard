export type User = {
  id: string;
  img: string;
  lastname: string;
  firstname: string;
  email: string;
  phone: string;
  createdAt: string;
  verified: string;
  amount: string;
};

export type Product = {
  id: string;
  img: string;
  title: string;
  color: string;
  producer: string;
  price: string;
  createdAt: string;
  inStock: string;
};
